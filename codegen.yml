overwrite: true
schema: "src/graphql/schema.ts"
documents: null
generates:
  src/graphql/tsdefs.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
