overwrite: true
schema: http://localhost:4000/api
documents: null
generates:
  src/graphql/tsdefs.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"